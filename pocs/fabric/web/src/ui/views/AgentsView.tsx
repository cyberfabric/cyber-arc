export default function AgentsView(): JSX.Element {
  return (
    <section className="view">
      <h1 className="view__title">Agents</h1>
      <p className="view__hint">Detected and registered agents will render here (next task).</p>
    </section>
  );
}
